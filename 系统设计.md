# 全国流感人群流动监测系统 - 系统设计文档

## 1. 项目概述

### 1.1 项目简介
全国流感人群流动监测系统是一个基于Web的全栈监测平台，用于实时监测、分析和可视化全国范围内流感人群的流动情况。系统提供数据统计、人员管理、态势感知等功能，帮助相关部门及时掌握疫情动态。

### 1.2 核心功能
- **数据概览**：实时展示监测人员总数、确诊病例、活跃区域等关键指标
- **人员档案管理**：支持人员信息的增删改查、多维度筛选和搜索
- **态势感知**：提供地区分布、趋势分析、状态分布等可视化图表
- **地图可视化**：基于ECharts的中国地图，展示各省份监测人数分布
- **重点人员管理**：支持重点人员的分类管理和优先级设置

### 1.3 技术特点
- 前后端分离架构
- RESTful API设计
- 响应式UI设计
- 实时数据可视化
- 模块化代码结构

## 2. 系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     前端层 (Frontend)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │  Vue 3   │  │ Vue Router│  │  ECharts │            │
│  └──────────┘  └──────────┘  └──────────┘            │
│         │              │              │                │
│         └──────────────┼──────────────┘                │
│                        │                                │
│                   HTTP/REST API                         │
└────────────────────────┼────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────┐
│                        │    后端层 (Backend)             │
│  ┌─────────────────────┼─────────────────────┐          │
│  │     Flask Web Server                      │          │
│  │  ┌──────────┐  ┌──────────┐  ┌─────────┐ │          │
│  │  │   API    │  │  CORS    │  │  Utils  │ │          │
│  │  │  Routes  │  │  Middleware│ │ Scripts│ │          │
│  │  └──────────┘  └──────────┘  └─────────┘ │          │
│  └───────────────────────────────────────────┘          │
│                        │                                │
│  ┌─────────────────────┼─────────────────────┐          │
│  │         Database Layer                    │          │
│  │  ┌─────────────────────────────────────┐ │          │
│  │  │      SQLite Database                 │ │          │
│  │  │  (可扩展支持 MySQL/PostgreSQL)        │ │          │
│  │  └─────────────────────────────────────┘ │          │
│  └───────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

#### 前端技术栈
- **框架**: Vue 3 (Composition API)
- **路由**: Vue Router 4
- **HTTP客户端**: Axios
- **图表库**: ECharts 5 + vue-echarts
- **构建工具**: Vite 5
- **开发语言**: JavaScript (ES6+)

#### 后端技术栈
- **Web框架**: Flask 3.0
- **跨域处理**: Flask-CORS
- **数据库**: SQLite 3 (默认)
- **数据生成**: Faker
- **图像处理**: Pillow
- **开发语言**: Python 3.7+

### 2.3 项目结构

```
.
├── frontend/                    # 前端项目
│   ├── src/
│   │   ├── views/              # 页面组件
│   │   │   ├── Home.vue        # 首页
│   │   │   ├── Archive.vue     # 人员档案
│   │   │   ├── PersonDetail.vue # 人员详情
│   │   │   ├── Situation.vue   # 态势感知
│   │   │   ├── KeyPersons.vue  # 重点人员
│   │   │   └── Workspace.vue   # 工作台
│   │   ├── router/             # 路由配置
│   │   │   └── index.js
│   │   ├── api/                # API接口封装
│   │   │   └── index.js
│   │   ├── utils/              # 工具函数
│   │   │   └── mapLoader.js    # 地图加载器
│   │   ├── App.vue             # 根组件
│   │   ├── main.js             # 入口文件
│   │   └── style.css           # 全局样式
│   ├── index.html
│   ├── package.json
│   └── vite.config.js          # Vite配置
│
├── backend/                     # 后端项目
│   ├── app.py                  # Flask主应用
│   ├── database.py             # 数据库接口
│   ├── init_database.py        # 数据库初始化
│   ├── init_key_persons.py     # 重点人员初始化
│   ├── map_sync.py             # 地图数据同步
│   ├── update_people_data.py   # 人员数据更新
│   ├── update_visit_records.py # 到访记录更新
│   ├── generate_avatars.py     # 头像生成
│   ├── run.py                  # 启动脚本
│   ├── requirements.txt        # Python依赖
│   ├── data/                   # 数据目录
│   │   └── flu_monitoring.db   # SQLite数据库
│   └── avatars/                # 头像目录
│
├── start.bat                   # Windows启动脚本
├── start.sh                    # Linux/Mac启动脚本
└── README.md                   # 项目说明
```

## 3. 数据库设计

### 3.1 数据库选型
- **默认数据库**: SQLite 3
- **数据库文件**: `backend/data/flu_monitoring.db`
- **扩展性**: 支持迁移到MySQL/PostgreSQL

### 3.2 数据表设计

#### 3.2.1 people 表（人员表）
存储人员基本信息及扩展信息

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| name | TEXT | NOT NULL | 姓名 |
| id_card | TEXT | UNIQUE, NOT NULL | 身份证号 |
| region | TEXT | NOT NULL | 地区 |
| hometown | TEXT | | 籍贯 |
| age | INTEGER | NOT NULL | 年龄 |
| phone | TEXT | NOT NULL | 电话 |
| status | TEXT | NOT NULL | 状态（正常/疑似/确诊/康复） |
| gender | TEXT | | 性别 |
| occupation | TEXT | | 职业 |
| avatar | TEXT | | 头像路径 |
| tags | TEXT | | 标签（JSON格式） |
| education_history | TEXT | | 教育经历（JSON格式） |
| work_history | TEXT | | 工作经历（JSON格式） |
| social_media | TEXT | | 社交媒体动态（JSON格式） |
| visit_records | TEXT | | 到访记录（JSON格式） |
| flight_records | TEXT | | 航班记录（JSON格式） |
| train_records | TEXT | | 铁路记录（JSON格式） |
| last_update | TEXT | | 最后更新时间 |
| created_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- `idx_people_region`: 地区索引
- `idx_people_status`: 状态索引

#### 3.2.2 movements 表（流动记录表）
存储人员流动记录

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| person_id | INTEGER | NOT NULL, FOREIGN KEY | 人员ID |
| person_name | TEXT | | 人员姓名 |
| avatar | TEXT | | 头像路径 |
| from_region | TEXT | NOT NULL | 出发地 |
| to_region | TEXT | NOT NULL | 目的地 |
| movement_time | TEXT | NOT NULL | 流动时间 |
| status | TEXT | NOT NULL | 状态 |
| created_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- `idx_movements_person`: 人员ID索引
- `idx_movements_time`: 时间索引

#### 3.2.3 map_data 表（地图数据表）
存储各省份的监测人数

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| province_name | TEXT | UNIQUE, NOT NULL | 省份名称 |
| value | INTEGER | NOT NULL, DEFAULT 0 | 监测人数 |
| updated_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

#### 3.2.4 trend_data 表（趋势数据表）
存储每日的趋势数据

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| date | TEXT | NOT NULL, UNIQUE | 日期 |
| confirmed_count | INTEGER | NOT NULL, DEFAULT 0 | 确诊人数 |
| suspected_count | INTEGER | NOT NULL, DEFAULT 0 | 疑似人数 |
| recovered_count | INTEGER | NOT NULL, DEFAULT 0 | 康复人数 |
| created_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- `idx_trend_data_date`: 日期索引

#### 3.2.5 flow_statistics 表（流动统计表）
存储各地区的流动数量

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| region | TEXT | NOT NULL | 地区 |
| flow_count | INTEGER | NOT NULL, DEFAULT 0 | 流动数量 |
| period | TEXT | NOT NULL | 统计周期（today/week/month） |
| updated_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**唯一约束**: (region, period)

**索引**:
- `idx_flow_statistics_region`: 地区索引

#### 3.2.6 key_persons 表（重点人员表）
存储重点人员信息

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键 |
| person_id | INTEGER | NOT NULL, FOREIGN KEY | 人员ID |
| category | TEXT | NOT NULL | 类别（高风险/密切接触者/疑似病例等） |
| priority_level | INTEGER | DEFAULT 1 | 优先级（1-5） |
| reason | TEXT | | 原因说明 |
| added_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 添加时间 |
| updated_at | TEXT | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**唯一约束**: (person_id, category)

**索引**:
- `idx_key_persons_category`: 类别索引
- `idx_key_persons_person`: 人员ID索引

### 3.3 数据关系图

```
people (1) ──────< (N) movements
  │
  │ (1)
  │
  └───────< (N) key_persons

map_data (独立表)
trend_data (独立表)
flow_statistics (独立表)
```

## 4. API设计

### 4.1 API规范
- **协议**: HTTP/HTTPS
- **数据格式**: JSON
- **字符编码**: UTF-8
- **跨域**: 支持CORS

### 4.2 API端点

#### 4.2.1 根路由
```
GET /
```
返回API基本信息

**响应示例**:
```json
{
  "message": "全国流感人群流动监测系统 API",
  "version": "1.0.0",
  "endpoints": {
    "stats": "/api/stats",
    "people": "/api/people",
    "situation": "/api/situation",
    "avatars": "/api/avatars/<filename>"
  }
}
```

#### 4.2.2 统计数据
```
GET /api/stats
```
获取系统统计数据

**响应示例**:
```json
{
  "stats": {
    "totalPeople": 8000,
    "confirmedCases": 2009,
    "activeRegions": 147,
    "todayMovements": 0
  },
  "recentActivities": [
    {
      "id": 1,
      "name": "张三",
      "avatar": "/api/avatars/avatar_1.jpg",
      "action": "从北京市流动到上海市",
      "time": "2025-12-11 10:30:00"
    }
  ],
  "mapData": [
    {
      "name": "北京市",
      "value": 582
    }
  ]
}
```

#### 4.2.3 人员管理

**获取人员列表**
```
GET /api/people
```

**创建人员**
```
POST /api/people
Content-Type: application/json

{
  "name": "张三",
  "idCard": "110101199001011234",
  "hometown": "北京市",
  "age": 30,
  "phone": "13800138000",
  "status": "正常"
}
```

**获取人员详情**
```
GET /api/people/:id
```

**更新人员**
```
PUT /api/people/:id
Content-Type: application/json

{
  "age": 31,
  "status": "疑似"
}
```

**删除人员**
```
DELETE /api/people/:id
```

#### 4.2.4 态势感知
```
GET /api/situation?period=week
```
获取态势感知数据

**查询参数**:
- `period`: 时间周期（today/week/month/all）

**响应示例**:
```json
{
  "regionData": [
    {"name": "北京市", "value": 582}
  ],
  "trendDates": ["2025-12-05", "2025-12-06"],
  "confirmedTrend": [10, 15],
  "suspectedTrend": [20, 25],
  "recoveredTrend": [5, 8],
  "statusDistribution": [
    {"value": 5000, "name": "正常"},
    {"value": 2000, "name": "疑似"},
    {"value": 800, "name": "确诊"},
    {"value": 200, "name": "康复"}
  ],
  "flowRegions": ["北京市", "上海市"],
  "flowCounts": [100, 80],
  "movements": [...]
}
```

#### 4.2.5 静态资源
```
GET /api/avatars/<filename>
```
提供头像图片服务

### 4.3 错误处理

**标准错误响应格式**:
```json
{
  "error": "错误描述",
  "message": "详细错误信息"
}
```

**HTTP状态码**:
- `200`: 成功
- `201`: 创建成功
- `400`: 请求参数错误
- `404`: 资源不存在
- `500`: 服务器内部错误

## 5. 前端架构设计

### 5.1 组件结构

```
App.vue (根组件)
├── 导航栏
└── RouterView
    ├── Home.vue (首页)
    │   ├── 统计卡片
    │   ├── 地图可视化
    │   └── 最近动态
    ├── Archive.vue (人员档案)
    │   ├── 搜索筛选
    │   ├── 标签筛选
    │   └── 人员卡片列表
    ├── PersonDetail.vue (人员详情)
    │   ├── 基本信息
    │   ├── 教育经历
    │   ├── 工作经历
    │   └── 流动记录
    ├── Situation.vue (态势感知)
    │   ├── 地区分布图
    │   ├── 趋势分析图
    │   ├── 状态分布图
    │   └── 流动轨迹图
    ├── KeyPersons.vue (重点人员)
    └── Workspace.vue (工作台)
```

### 5.2 状态管理
- 使用Vue 3 Composition API的`ref`和`reactive`进行组件级状态管理
- 通过API调用获取服务端数据
- 使用`computed`实现响应式计算

### 5.3 路由设计

| 路径 | 组件 | 说明 |
|------|------|------|
| `/` | Home | 首页 |
| `/archive` | Archive | 人员档案列表 |
| `/archive/person/:id` | PersonDetail | 人员详情 |
| `/situation` | Situation | 态势感知 |
| `/key-persons` | KeyPersons | 重点人员 |
| `/workspace` | Workspace | 工作台 |

### 5.4 数据流

```
用户操作
  ↓
组件事件处理
  ↓
API调用 (api/index.js)
  ↓
HTTP请求 (Axios)
  ↓
后端API (Flask)
  ↓
数据库操作 (SQLite)
  ↓
响应数据
  ↓
组件状态更新
  ↓
UI渲染
```

## 6. 后端架构设计

### 6.1 模块划分

#### 6.1.1 应用层 (app.py)
- Flask应用初始化
- 路由定义
- 请求处理
- 错误处理

#### 6.1.2 数据访问层 (database.py)
- 数据库连接管理
- 表结构创建
- CRUD操作封装
- 数据转换

#### 6.1.3 业务逻辑层
- `init_database.py`: 数据初始化
- `map_sync.py`: 地图数据同步
- `update_people_data.py`: 人员数据更新
- `init_key_persons.py`: 重点人员初始化

#### 6.1.4 工具层
- `generate_avatars.py`: 头像生成
- `test_api.py`: API测试

### 6.2 请求处理流程

```
HTTP请求
  ↓
Flask路由匹配
  ↓
中间件处理 (CORS)
  ↓
数据库连接获取 (get_db)
  ↓
业务逻辑处理
  ↓
数据库操作
  ↓
数据格式化
  ↓
JSON响应
  ↓
数据库连接关闭 (teardown_appcontext)
```

### 6.3 线程安全
- 使用Flask的`g`对象实现线程本地存储
- 每个请求独立的数据库连接
- 请求结束时自动关闭连接

## 7. 安全设计

### 7.1 当前安全措施
- CORS跨域保护
- SQL注入防护（使用参数化查询）
- 输入验证（前端+后端）

### 7.2 建议的安全增强
- 用户认证和授权（JWT）
- HTTPS加密传输
- 数据加密存储
- API限流
- 日志审计

## 8. 性能优化

### 8.1 数据库优化
- 索引优化（已创建关键字段索引）
- 查询优化（避免N+1查询）
- 连接池管理

### 8.2 前端优化
- 组件懒加载
- 图片懒加载
- 数据分页
- 防抖和节流

### 8.3 缓存策略
- 静态资源缓存
- API响应缓存（可扩展）

## 9. 扩展性设计

### 9.1 数据库扩展
- 支持MySQL/PostgreSQL迁移
- 数据库接口抽象，易于切换

### 9.2 功能扩展
- 模块化设计，易于添加新功能
- API版本控制支持
- 插件化架构

### 9.3 部署扩展
- 支持Docker容器化
- 支持微服务拆分
- 支持负载均衡

## 10. 监控与日志

### 10.1 日志记录
- 请求日志
- 错误日志
- 数据库操作日志

### 10.2 监控指标
- API响应时间
- 数据库查询性能
- 系统资源使用

## 11. 部署架构

### 11.1 开发环境
```
前端: Vite Dev Server (localhost:3000)
后端: Flask Dev Server (localhost:8000)
数据库: SQLite (本地文件)
```

### 11.2 生产环境建议
```
前端: Nginx (静态资源) + CDN
后端: Gunicorn/uWSGI + Nginx (反向代理)
数据库: PostgreSQL/MySQL (独立服务器)
```

## 12. 版本信息

- **当前版本**: v1.0.0
- **最后更新**: 2025-12-11
- **维护状态**: 活跃开发中

---

**文档维护**: 本文档随系统演进持续更新，请关注版本变更。

