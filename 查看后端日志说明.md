# 如何查看后端控制台日志

## 方法1：查看现有的后端服务窗口

1. **查找后端服务窗口**
   - 如果通过双击 `start_backend.bat` 启动，会弹出一个命令行窗口
   - 这个窗口标题通常显示为 "启动后端服务" 或类似内容
   - 日志会实时显示在这个窗口中

2. **日志内容说明**
   - `[DEBUG]` - 调试信息（包括数据导入的详细信息）
   - `[WARN]` - 警告信息（如字段缺失等）
   - `[ERROR]` - 错误信息
   - `[OK]` - 成功信息

## 方法2：重新启动后端服务查看日志

如果找不到后端服务窗口，可以重新启动：

1. **停止当前后端服务**
   - 找到运行后端的命令行窗口，按 `Ctrl+C` 停止
   - 或者关闭后端服务的命令行窗口

2. **重新启动后端服务**
   - 双击 `start_backend_with_logs.bat`（新创建的脚本，会显示详细日志）
   - 或者双击 `start_backend.bat`

3. **查看日志**
   - 导入文件时，后端控制台会显示：
     ```
     [DEBUG] Excel文件读取成功，共 X 行，列名: [...]
     [DEBUG] 删除空行后，剩余 X 行
     [DEBUG] 转换后的第一条数据: {...}
     [DEBUG] 第一条数据的name字段值: ..., 类型: ...
     [DEBUG] 处理第 1 条数据，字段: [...]
     [DEBUG] 第 1 条数据验证通过，姓名: ...
     [DEBUG] 成功导入第 1 条数据: ...
     ```

## 方法3：使用终端启动（推荐）

1. 打开 PowerShell 或 CMD
2. 进入项目目录：`cd F:\all\projects\chaoyangV1.0.1`
3. 运行：`.\start_backend.bat`
4. 日志会直接显示在终端中

## 导入文件时的关键日志信息

导入Excel/CSV文件时，请关注以下日志：

- **文件读取阶段**：
  - `[DEBUG] Excel文件读取成功，共 X 行`
  - `[DEBUG] 删除空行后，剩余 X 行`
  - `[DEBUG] 第一条数据的name字段值: ...`

- **数据处理阶段**：
  - `[DEBUG] 处理第 X 条数据，字段: [...]`
  - `[DEBUG] 第 X 条数据验证通过，姓名: ...`
  - `[WARN] 第X条数据缺少必要字段：name（姓名）`（如果有问题）

- **数据库插入阶段**：
  - `[DEBUG] 成功导入第 X 条数据: ...`
  - `[DEBUG] 数据库提交成功`
  - `[DEBUG] 导入完成，成功 X 条，失败 X 条`

## 如果看不到日志窗口

如果后端服务是在后台运行的，可以：

1. 打开任务管理器（Ctrl+Shift+Esc）
2. 找到 `python.exe` 进程
3. 结束这些进程
4. 重新启动后端服务

