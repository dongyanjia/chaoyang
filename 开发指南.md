# 全国流感人群流动监测系统 - 开发文档

## 1. 开发环境搭建

### 1.1 系统要求

- **操作系统**: Windows 10+, Linux, macOS
- **Python**: 3.7 或更高版本
- **Node.js**: 14.0 或更高版本
- **npm**: 6.0 或更高版本（随Node.js安装）

### 1.2 环境检查

**检查Python版本**:
```bash
python --version
# 或
python3 --version
```

**检查Node.js版本**:
```bash
node --version
npm --version
```

### 1.3 项目克隆

```bash
git clone <repository-url>
cd 朝阳
```

### 1.4 后端环境搭建

**1. 进入后端目录**:
```bash
cd backend
```

**2. 创建虚拟环境（推荐）**:
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

**3. 安装依赖**:
```bash
pip install -r requirements.txt
```

**依赖包说明**:
- `flask==3.0.0`: Web框架
- `flask-cors==4.0.0`: 跨域支持
- `python-dotenv==1.0.0`: 环境变量管理
- `Pillow==10.1.0`: 图像处理
- `faker==20.1.0`: 假数据生成

### 1.5 前端环境搭建

**1. 进入前端目录**:
```bash
cd frontend
```

**2. 安装依赖**:
```bash
npm install
```

**依赖包说明**:
- `vue@^3.3.4`: Vue框架
- `vue-router@^4.2.5`: 路由管理
- `axios@^1.6.0`: HTTP客户端
- `echarts@^5.4.3`: 图表库
- `vue-echarts@^6.6.1`: Vue ECharts组件

**开发依赖**:
- `@vitejs/plugin-vue@^4.5.0`: Vite Vue插件
- `vite@^5.0.0`: 构建工具

## 2. 项目启动

### 2.1 快速启动（推荐）

**Windows**:
```bash
start.bat
```

**Linux/Mac**:
```bash
chmod +x start.sh
./start.sh
```

### 2.2 手动启动

**1. 启动后端服务**:
```bash
cd backend
python run.py
```

后端服务将在 `http://localhost:8000` 启动

**2. 启动前端服务（新终端）**:
```bash
cd frontend
npm run dev
```

前端服务将在 `http://localhost:3000` 启动

### 2.3 访问系统

打开浏览器访问: `http://localhost:3000`

## 3. 开发规范

### 3.1 代码风格

#### Python代码规范
- 遵循PEP 8规范
- 使用4个空格缩进
- 函数和类添加文档字符串
- 变量命名使用snake_case
- 类名使用PascalCase

**示例**:
```python
def get_person_by_id(person_id: int) -> Optional[Dict]:
    """根据ID获取人员信息
    
    Args:
        person_id: 人员ID
        
    Returns:
        人员信息字典，如果不存在返回None
    """
    # 实现代码
    pass
```

#### JavaScript代码规范
- 使用ES6+语法
- 使用2个空格缩进
- 变量命名使用camelCase
- 组件名使用PascalCase
- 使用const/let，避免var

**示例**:
```javascript
const fetchPeople = async () => {
  try {
    const response = await api.getPeople()
    people.value = response.data
  } catch (error) {
    console.error('获取人员列表失败:', error)
  }
}
```

### 3.2 文件命名规范

- **Python文件**: 使用snake_case，如 `database.py`, `init_database.py`
- **Vue组件**: 使用PascalCase，如 `Home.vue`, `PersonDetail.vue`
- **JavaScript工具文件**: 使用camelCase，如 `mapLoader.js`

### 3.3 Git提交规范

**提交信息格式**:
```
<type>: <subject>

[optional body]
```

**类型说明**:
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例**:
```bash
git commit -m "feat: 添加人员档案页加载进度条"
git commit -m "fix: 修复地图数据同步问题"
git commit -m "docs: 更新API文档"
```

## 4. 开发流程

### 4.1 功能开发流程

1. **创建功能分支**
```bash
git checkout -b feature/your-feature-name
```

2. **开发功能**
   - 编写代码
   - 添加必要的注释
   - 确保代码符合规范

3. **测试功能**
   - 手动测试功能
   - 检查控制台错误
   - 验证API响应

4. **提交代码**
```bash
git add .
git commit -m "feat: 功能描述"
```

5. **合并到主分支**
```bash
git checkout master
git merge feature/your-feature-name
```

### 4.2 数据库操作流程

**1. 初始化数据库**:
```bash
cd backend
python init_database.py
```

**2. 更新人员数据**:
```bash
python update_people_data.py
```

**3. 查看数据库**:
可以使用SQLite工具（如DB Browser for SQLite）打开 `backend/data/flu_monitoring.db`

### 4.3 API开发流程

**1. 定义API端点** (backend/app.py):
```python
@app.route('/api/your-endpoint', methods=['GET'])
def your_endpoint():
    """API说明"""
    db = get_db()
    # 业务逻辑
    return jsonify(data)
```

**2. 前端调用** (frontend/src/api/index.js):
```javascript
export default {
  yourMethod() {
    return api.get('/your-endpoint')
  }
}
```

**3. 在组件中使用**:
```javascript
import api from '../api'

const fetchData = async () => {
  try {
    const response = await api.yourMethod()
    data.value = response.data
  } catch (error) {
    console.error('错误:', error)
  }
}
```

## 5. 常用开发任务

### 5.1 添加新页面

**1. 创建Vue组件** (frontend/src/views/YourPage.vue):
```vue
<template>
  <div class="your-page">
    <!-- 页面内容 -->
  </div>
</template>

<script>
import { ref, onMounted } from 'vue'

export default {
  name: 'YourPage',
  setup() {
    // 组件逻辑
    return {
      // 返回的数据和方法
    }
  }
}
</script>

<style scoped>
.your-page {
  /* 样式 */
}
</style>
```

**2. 添加路由** (frontend/src/router/index.js):
```javascript
import YourPage from '../views/YourPage.vue'

const routes = [
  // ... 其他路由
  {
    path: '/your-page',
    name: 'YourPage',
    component: YourPage
  }
]
```

**3. 添加导航链接** (frontend/src/App.vue):
```vue
<router-link to="/your-page">新页面</router-link>
```

### 5.2 添加新的API接口

**1. 后端添加路由** (backend/app.py):
```python
@app.route('/api/your-endpoint', methods=['GET', 'POST'])
def your_endpoint():
    db = get_db()
    if request.method == 'GET':
        # GET逻辑
        data = db.get_something()
        return jsonify(data)
    elif request.method == 'POST':
        # POST逻辑
        data = request.json
        result = db.create_something(data)
        return jsonify(result), 201
```

**2. 前端封装API** (frontend/src/api/index.js):
```javascript
export default {
  // ... 其他方法
  getYourData() {
    return api.get('/your-endpoint')
  },
  createYourData(data) {
    return api.post('/your-endpoint', data)
  }
}
```

### 5.3 添加数据库表

**1. 在database.py中添加表创建逻辑**:
```python
def _create_tables(self):
    cursor = self.connection.cursor()
    
    # 添加新表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS your_table (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            name TEXT NOT NULL,
            created_at TEXT DEFAULT CURRENT_TIMESTAMP
        )
    ''')
    
    # 创建索引
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_your_table_name ON your_table(name)')
    
    self.connection.commit()
```

**2. 添加CRUD方法**:
```python
def get_your_data(self) -> List[Dict]:
    """获取数据"""
    cursor = self.connection.cursor()
    cursor.execute('SELECT * FROM your_table')
    rows = cursor.fetchall()
    return [self._row_to_dict(row) for row in rows]

def create_your_data(self, data: Dict) -> Dict:
    """创建数据"""
    cursor = self.connection.cursor()
    cursor.execute('''
        INSERT INTO your_table (name) VALUES (?)
    ''', (data['name'],))
    self.connection.commit()
    return self.get_your_data_by_id(cursor.lastrowid)
```

### 5.4 添加图表

**1. 在组件中引入ECharts**:
```vue
<script>
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { BarChart } from 'echarts/charts'
import { TitleComponent, TooltipComponent } from 'echarts/components'
import VChart from 'vue-echarts'

use([CanvasRenderer, BarChart, TitleComponent, TooltipComponent])

export default {
  components: {
    VChart
  },
  setup() {
    const chartOption = computed(() => ({
      // 图表配置
    }))
    
    return {
      chartOption
    }
  }
}
</script>

<template>
  <v-chart :option="chartOption" />
</template>
```

## 6. 调试技巧

### 6.1 前端调试

**1. 浏览器开发者工具**
- 打开Chrome DevTools (F12)
- 查看Console标签页的错误信息
- 使用Network标签页查看API请求
- 使用Vue DevTools插件调试组件状态

**2. 添加调试日志**:
```javascript
console.log('调试信息:', data)
console.error('错误信息:', error)
```

**3. 断点调试**
- 在代码中添加 `debugger` 语句
- 在浏览器中打开开发者工具
- 刷新页面，代码会在断点处暂停

### 6.2 后端调试

**1. 使用print语句**:
```python
print(f'调试信息: {variable}')
```

**2. 使用日志**:
```python
import logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)
logger.debug('调试信息')
```

**3. Flask调试模式**:
在 `app.py` 中:
```python
app.run(debug=True)
```

### 6.3 数据库调试

**1. 查看SQL查询**:
在 `database.py` 中添加:
```python
print(f'执行SQL: {query}')
print(f'参数: {params}')
```

**2. 使用SQLite命令行工具**:
```bash
sqlite3 backend/data/flu_monitoring.db
.tables
SELECT * FROM people LIMIT 10;
```

## 7. 测试

### 7.1 API测试

**使用test_api.py**:
```bash
cd backend
python test_api.py
```

**手动测试**:
```bash
# 使用curl
curl http://localhost:8000/api/stats

# 使用PowerShell
Invoke-WebRequest -Uri http://localhost:8000/api/stats
```

### 7.2 前端测试

**在浏览器中测试**:
1. 打开 `http://localhost:3000`
2. 测试各个页面功能
3. 检查控制台错误
4. 验证数据加载

## 8. 常见问题

### 8.1 后端无法启动

**问题**: 端口被占用
**解决**:
```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <进程ID> /F

# Linux/Mac
lsof -ti:8000 | xargs kill
```

**问题**: 依赖未安装
**解决**:
```bash
cd backend
pip install -r requirements.txt
```

### 8.2 前端无法启动

**问题**: 端口被占用
**解决**: 修改 `vite.config.js` 中的端口号

**问题**: 依赖未安装
**解决**:
```bash
cd frontend
npm install
```

### 8.3 数据库连接错误

**问题**: 数据库文件不存在
**解决**: 运行初始化脚本
```bash
cd backend
python init_database.py
```

**问题**: 数据库文件权限错误
**解决**: 检查文件权限，确保有读写权限

### 8.4 API请求失败

**问题**: CORS错误
**解决**: 确保后端已启用CORS，检查 `app.py` 中的CORS配置

**问题**: 404错误
**解决**: 
- 检查后端服务是否运行
- 检查API路径是否正确
- 检查Vite代理配置

### 8.5 地图不显示

**问题**: 地图数据未加载
**解决**:
- 检查浏览器控制台错误
- 确认地图JSON文件可访问
- 检查 `mapLoader.js` 中的加载逻辑

## 9. 性能优化建议

### 9.1 前端优化

**1. 组件懒加载**:
```javascript
const Archive = () => import('../views/Archive.vue')
```

**2. 图片懒加载**:
```vue
<img v-lazy="imageUrl" />
```

**3. 数据分页**:
```javascript
const pageSize = 20
const currentPage = ref(1)
const paginatedData = computed(() => {
  const start = (currentPage.value - 1) * pageSize
  return data.value.slice(start, start + pageSize)
})
```

### 9.2 后端优化

**1. 数据库查询优化**:
- 使用索引
- 避免N+1查询
- 使用批量操作

**2. 缓存策略**:
```python
from functools import lru_cache

@lru_cache(maxsize=100)
def get_cached_data():
    # 缓存数据
    pass
```

## 10. 部署准备

### 10.1 前端构建

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist` 目录

### 10.2 后端配置

**生产环境配置**:
- 关闭调试模式
- 配置数据库连接
- 设置环境变量
- 配置日志

### 10.3 环境变量

创建 `.env` 文件:
```env
FLASK_ENV=production
DATABASE_URL=sqlite:///data/flu_monitoring.db
SECRET_KEY=your-secret-key
```

## 11. 代码审查清单

提交代码前检查:

- [ ] 代码符合规范
- [ ] 添加了必要的注释
- [ ] 测试了功能
- [ ] 检查了控制台错误
- [ ] 更新了相关文档
- [ ] 提交信息清晰

## 12. 资源链接

- **Vue 3文档**: https://vuejs.org/
- **Flask文档**: https://flask.palletsprojects.com/
- **ECharts文档**: https://echarts.apache.org/
- **SQLite文档**: https://www.sqlite.org/docs.html

## 13. 获取帮助

- 查看项目README.md
- 查看系统设计文档 (SYSTEM_DESIGN.md)
- 查看数据库文档 (backend/DATABASE_README.md)
- 提交Issue到项目仓库

---

**最后更新**: 2025-12-11  
**维护者**: 开发团队

