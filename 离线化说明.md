# 系统离线化说明

## 概述
本项目已完全离线化，所有功能均可在无互联网连接的环境下正常运行。

## 离线化检查清单

### ✅ 已完成

#### 1. 地图数据离线化
- **位置**: `frontend/src/utils/mapLoader.js`
- **修改内容**: 
  - 移除了所有在线地图数据源（阿里云、GitHub）
  - 仅使用本地地图文件：`/maps/china.json`
  - 如果本地文件不存在，会显示明确的错误提示，不再尝试在线加载
- **本地文件**: `frontend/public/maps/china.json` (约569KB)

#### 2. 后端API检查
- **检查结果**: 后端代码中无任何外部API调用
- **所有数据**: 均来自本地SQLite数据库
- **API端点**: 全部为本地服务（localhost:8000）

#### 3. 前端资源检查
- **HTML文件**: 无CDN链接，无外部资源引用
- **JavaScript**: 所有依赖包已通过npm安装到本地
- **API调用**: 全部指向本地后端服务（通过Vite代理）

### 📋 不需要修改的部分

#### 1. package-lock.json 中的 npm registry URL
- **说明**: 这些URL仅在安装依赖时使用（`npm install`）
- **运行时**: 不影响系统运行，所有依赖已下载到本地 `node_modules`
- **状态**: 无需修改

#### 2. localhost:8000 本地服务地址
- **说明**: 这是本地后端服务地址，不是互联网资源
- **用途**: 前端通过Vite代理连接到本地Flask后端
- **状态**: 无需修改

#### 3. 测试脚本中的 localhost 地址
- **文件**: `backend/test_api.py`, `backend/check_server.py`
- **说明**: 这些是本地测试脚本，用于测试本地服务
- **状态**: 无需修改

## 离线运行要求

### 必需文件
1. **地图数据文件**: `frontend/public/maps/china.json`
   - 如果文件不存在，地图功能将无法使用
   - 文件大小：约569KB

2. **数据库文件**: `backend/data/flu_monitoring.db`
   - SQLite数据库文件
   - 首次运行会自动创建

3. **依赖包**:
   - 前端：`frontend/node_modules/`（通过 `npm install` 安装）
   - 后端：Python虚拟环境中的包（通过 `pip install -r requirements.txt` 安装）

### 启动步骤（完全离线）

1. **启动后端服务**:
   ```bash
   cd backend
   python app.py
   # 或使用启动脚本
   start_backend.bat
   ```

2. **启动前端服务**:
   ```bash
   cd frontend
   npm run dev
   ```

3. **访问系统**:
   - 前端地址: http://localhost:3000
   - 后端API: http://localhost:8000

## 验证离线功能

### 测试步骤
1. 断开网络连接
2. 启动后端和前端服务
3. 访问系统，验证以下功能：
   - ✅ 地图显示（使用本地地图数据）
   - ✅ 数据查询（从本地数据库）
   - ✅ 文件导入（Excel/CSV/JSON）
   - ✅ 所有API接口（本地服务）

### 预期结果
- 所有功能正常工作
- 无任何网络请求错误
- 地图正常显示（使用本地数据）

## 注意事项

1. **地图数据文件**: 
   - 确保 `frontend/public/maps/china.json` 文件存在
   - 如果文件丢失，需要重新下载或从备份恢复

2. **首次安装**:
   - 首次安装依赖时需要网络连接（`npm install`, `pip install`）
   - 安装完成后即可离线运行

3. **数据备份**:
   - 建议定期备份数据库文件 `backend/data/flu_monitoring.db`
   - 建议备份地图数据文件 `frontend/public/maps/china.json`

## 修改记录

- **2024-01-04**: 完成地图数据离线化，移除所有在线备用数据源
- **2024-01-04**: 添加新闻热点和社交动态API端点（使用本地数据库数据）

## 总结

✅ **系统已完全离线化**
- 无外部API依赖
- 无在线资源引用
- 所有数据来自本地
- 可在完全离线环境下运行

