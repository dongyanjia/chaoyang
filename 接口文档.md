# 全国流感人群流动监测系统 - API接口文档

## 1. 概述

### 1.1 基本信息
- **API版本**: v1.0.0
- **基础URL**: `http://localhost:8000`
- **协议**: HTTP/HTTPS
- **数据格式**: JSON
- **字符编码**: UTF-8

### 1.2 请求规范
- **请求头**: `Content-Type: application/json`
- **请求方法**: GET, POST, PUT, DELETE
- **跨域**: 支持CORS

### 1.3 响应规范

**成功响应格式**:
```json
{
  "data": "响应数据"
}
```

**错误响应格式**:
```json
{
  "error": "错误描述",
  "message": "详细错误信息"
}
```

### 1.4 HTTP状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 201 | 创建成功 |
| 400 | 请求参数错误 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

## 2. 接口列表

### 2.1 根路由

#### 获取API信息
获取API基本信息和可用端点列表。

**请求**
```
GET /
```

**响应示例**
```json
{
  "message": "全国流感人群流动监测系统 API",
  "version": "1.0.0",
  "endpoints": {
    "stats": "/api/stats",
    "people": "/api/people",
    "situation": "/api/situation",
    "avatars": "/api/avatars/<filename>"
  }
}
```

---

## 3. 统计数据接口

### 3.1 获取统计数据

获取系统统计数据，包括人员总数、确诊病例、活跃区域、今日流动记录、最近动态、地图数据和省份排名。

**请求**
```
GET /api/stats
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| stats | Object | 统计数据对象 |
| stats.totalPeople | Integer | 监测人员总数 |
| stats.confirmedCases | Integer | 确诊病例数 |
| stats.activeRegions | Integer | 活跃区域数 |
| stats.todayMovements | Integer | 今日流动记录数 |
| recentActivities | Array | 最近动态列表 |
| recentActivities[].id | Integer | 动态ID |
| recentActivities[].name | String | 人员姓名 |
| recentActivities[].avatar | String | 头像路径 |
| recentActivities[].action | String | 动态描述 |
| recentActivities[].time | String | 时间 |
| mapData | Array | 地图数据 |
| mapData[].name | String | 省份名称 |
| mapData[].value | Integer | 监测人数 |
| provinceRanking | Array | 省份确诊排名 |

**响应示例**
```json
{
  "stats": {
    "totalPeople": 8000,
    "confirmedCases": 2009,
    "activeRegions": 147,
    "todayMovements": 0
  },
  "recentActivities": [
    {
      "id": 1,
      "name": "张三",
      "avatar": "/api/avatars/avatar_1.jpg",
      "action": "从北京市流动到上海市",
      "time": "2025-12-11 10:30:00"
    }
  ],
  "mapData": [
    {
      "name": "北京市",
      "value": 582
    },
    {
      "name": "上海市",
      "value": 582
    }
  ],
  "provinceRanking": [
    {
      "province": "北京市",
      "confirmedCount": 150,
      "totalCount": 582
    }
  ]
}
```

---

## 4. 人员管理接口

### 4.1 获取人员列表

获取所有人员信息列表。

**请求**
```
GET /api/people
```

**响应示例**
```json
[
  {
    "id": 1,
    "name": "张三",
    "idCard": "110101199001011234",
    "region": "北京市",
    "hometown": "北京市",
    "age": 30,
    "phone": "13800138000",
    "status": "正常",
    "gender": "男",
    "occupation": "工程师",
    "avatar": "/api/avatars/avatar_1.jpg",
    "tags": ["技术", "北京"],
    "educationHistory": [
      {
        "school": "北京大学",
        "degree": "本科",
        "major": "计算机科学",
        "startDate": "2010-09",
        "endDate": "2014-06"
      }
    ],
    "workHistory": [
      {
        "company": "某科技公司",
        "position": "软件工程师",
        "startDate": "2014-07",
        "endDate": "2018-12"
      }
    ],
    "visitRecords": [
      {
        "province": "上海市",
        "city": "上海市",
        "visitDate": "2025-11-15"
      }
    ],
    "flightRecords": [
      {
        "airline": "中国国际航空",
        "flightNumber": "CA1234",
        "from": "北京",
        "to": "上海",
        "date": "2025-11-15"
      }
    ],
    "trainRecords": [
      {
        "type": "高铁",
        "trainNumber": "G123",
        "from": "北京",
        "to": "上海",
        "date": "2025-11-20"
      }
    ],
    "socialMedia": [
      {
        "platform": "微信",
        "account": "zhangsan",
        "lastActive": "2025-12-10"
      }
    ],
    "lastUpdate": "2025-12-11 10:30:00",
    "createdAt": "2025-01-01 00:00:00"
  }
]
```

### 4.2 获取人员详情

根据ID获取单个人员的详细信息。

**请求**
```
GET /api/people/:id
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | Integer | 是 | 人员ID |

**响应示例**
```json
{
  "id": 1,
  "name": "张三",
  "idCard": "110101199001011234",
  "region": "北京市",
  "hometown": "北京市",
  "age": 30,
  "phone": "13800138000",
  "status": "正常",
  "gender": "男",
  "occupation": "工程师",
  "avatar": "/api/avatars/avatar_1.jpg",
  "tags": ["技术", "北京"],
  "educationHistory": [...],
  "workHistory": [...],
  "visitRecords": [...],
  "flightRecords": [...],
  "trainRecords": [...],
  "socialMedia": [...],
  "lastUpdate": "2025-12-11 10:30:00",
  "createdAt": "2025-01-01 00:00:00"
}
```

**错误响应** (404)
```json
{
  "error": "人员不存在"
}
```

### 4.3 创建人员

创建新的人员记录。

**请求**
```
POST /api/people
Content-Type: application/json
```

**请求体参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 姓名 |
| idCard | String | 是 | 身份证号（唯一） |
| hometown | String | 是 | 籍贯 |
| age | Integer | 是 | 年龄 |
| phone | String | 是 | 电话 |
| status | String | 是 | 状态（正常/疑似/确诊/康复） |
| region | String | 否 | 地区 |
| gender | String | 否 | 性别 |
| occupation | String | 否 | 职业 |
| avatar | String | 否 | 头像路径（不提供则随机分配） |
| tags | Array | 否 | 标签列表 |
| educationHistory | Array | 否 | 教育经历 |
| workHistory | Array | 否 | 工作经历 |
| visitRecords | Array | 否 | 到访记录 |
| flightRecords | Array | 否 | 航班记录 |
| trainRecords | Array | 否 | 铁路记录 |
| socialMedia | Array | 否 | 社交媒体动态 |

**请求示例**
```json
{
  "name": "李四",
  "idCard": "110101199002021234",
  "hometown": "北京市",
  "age": 29,
  "phone": "13900139000",
  "status": "正常",
  "gender": "女",
  "occupation": "医生"
}
```

**响应示例** (201)
```json
{
  "id": 8001,
  "name": "李四",
  "idCard": "110101199002021234",
  "hometown": "北京市",
  "age": 29,
  "phone": "13900139000",
  "status": "正常",
  "gender": "女",
  "occupation": "医生",
  "avatar": "/api/avatars/avatar_5.jpg",
  "lastUpdate": "2025-12-11 15:30:00",
  "createdAt": "2025-12-11 15:30:00"
}
```

### 4.4 更新人员

更新现有人员信息。

**请求**
```
PUT /api/people/:id
Content-Type: application/json
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | Integer | 是 | 人员ID |

**请求体参数**

所有字段均为可选，只传递需要更新的字段。

**请求示例**
```json
{
  "age": 31,
  "status": "疑似",
  "phone": "13800138001"
}
```

**响应示例**
```json
{
  "id": 1,
  "name": "张三",
  "age": 31,
  "status": "疑似",
  "phone": "13800138001",
  "lastUpdate": "2025-12-11 16:00:00"
}
```

**错误响应** (404)
```json
{
  "error": "人员不存在"
}
```

### 4.5 删除人员

删除指定人员记录。

**请求**
```
DELETE /api/people/:id
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | Integer | 是 | 人员ID |

**响应示例** (200)
```json
{
  "message": "删除成功"
}
```

**错误响应** (404)
```json
{
  "error": "人员不存在"
}
```

---

## 5. 重点人员管理接口

### 5.1 获取重点人员列表

获取重点人员列表，支持按类别筛选。

**请求**
```
GET /api/key-persons?category=高风险人员
```

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | String | 否 | 类别（高风险人员/密切接触者/疑似病例/确诊病例/境外输入/其他），不传或传"全部"则返回所有 |

**响应示例**
```json
[
  {
    "id": 1,
    "keyPersonId": 1,
    "name": "张三",
    "idCard": "110101199001011234",
    "region": "北京市",
    "hometown": "北京市",
    "age": 30,
    "phone": "13800138000",
    "status": "确诊",
    "avatar": "/api/avatars/avatar_1.jpg",
    "gender": "男",
    "occupation": "工程师",
    "category": "高风险人员",
    "priorityLevel": 5,
    "reason": "近期有高风险地区旅居史",
    "addedAt": "2025-12-01 10:00:00",
    "updatedAt": "2025-12-01 10:00:00"
  }
]
```

### 5.2 获取重点人员类别

获取所有可用的重点人员类别。

**请求**
```
GET /api/key-persons/categories
```

**响应示例**
```json
[
  "高风险人员",
  "密切接触者",
  "疑似病例",
  "确诊病例",
  "境外输入",
  "其他"
]
```

### 5.3 添加重点人员

将人员添加到重点人员列表。

**请求**
```
POST /api/key-persons
Content-Type: application/json
```

**请求体参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| personId | Integer | 是 | 人员ID |
| category | String | 是 | 类别 |
| priorityLevel | Integer | 否 | 优先级（1-5，默认1） |
| reason | String | 否 | 原因说明 |

**请求示例**
```json
{
  "personId": 1,
  "category": "高风险人员",
  "priorityLevel": 5,
  "reason": "近期有高风险地区旅居史"
}
```

**响应示例** (201)
```json
{
  "id": 1,
  "personId": 1,
  "category": "高风险人员",
  "priorityLevel": 5,
  "reason": "近期有高风险地区旅居史",
  "addedAt": "2025-12-11 16:00:00",
  "updatedAt": "2025-12-11 16:00:00"
}
```

**错误响应** (400)
```json
{
  "error": "缺少必要参数"
}
```

### 5.4 更新重点人员信息

更新重点人员的信息。

**请求**
```
PUT /api/key-persons/:person_id
Content-Type: application/json
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| person_id | Integer | 是 | 人员ID |

**请求体参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | String | 是 | 类别 |
| priorityLevel | Integer | 否 | 优先级 |
| reason | String | 否 | 原因说明 |

**请求示例**
```json
{
  "category": "高风险人员",
  "priorityLevel": 4,
  "reason": "更新原因说明"
}
```

**响应示例**
```json
{
  "id": 1,
  "personId": 1,
  "category": "高风险人员",
  "priorityLevel": 4,
  "reason": "更新原因说明",
  "updatedAt": "2025-12-11 16:30:00"
}
```

**错误响应** (400)
```json
{
  "error": "缺少类别参数"
}
```

**错误响应** (404)
```json
{
  "error": "更新失败"
}
```

### 5.5 移除重点人员

从重点人员列表中移除指定人员。

**请求**
```
DELETE /api/key-persons/:person_id?category=高风险人员
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| person_id | Integer | 是 | 人员ID |

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | String | 是 | 类别 |

**响应示例** (200)
```json
{
  "message": "移除成功"
}
```

**错误响应** (400)
```json
{
  "error": "缺少类别参数"
}
```

**错误响应** (404)
```json
{
  "error": "移除失败"
}
```

---

## 6. 态势感知接口

### 6.1 获取态势感知数据

获取态势感知相关的数据，包括地区分布、趋势分析、状态分布、流动统计等。

**请求**
```
GET /api/situation?period=week
```

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| period | String | 否 | 时间周期（today/week/month/all），默认week |

**period参数说明**:
- `today`: 今日数据
- `week`: 最近7天数据
- `month`: 最近30天数据
- `all`: 全部数据

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| regionData | Array | 地区分布数据 |
| regionData[].name | String | 地区名称 |
| regionData[].value | Integer | 人员数量 |
| trendDates | Array | 趋势日期列表 |
| confirmedTrend | Array | 确诊趋势数据 |
| suspectedTrend | Array | 疑似趋势数据 |
| recoveredTrend | Array | 康复趋势数据 |
| statusDistribution | Array | 状态分布数据 |
| statusDistribution[].name | String | 状态名称 |
| statusDistribution[].value | Integer | 数量 |
| flowRegions | Array | 流动地区列表 |
| flowCounts | Array | 流动数量列表 |
| movements | Array | 流动记录列表（最多50条） |

**响应示例**
```json
{
  "regionData": [
    {
      "name": "北京市",
      "value": 582
    },
    {
      "name": "上海市",
      "value": 582
    }
  ],
  "trendDates": [
    "2025-12-05",
    "2025-12-06",
    "2025-12-07",
    "2025-12-08",
    "2025-12-09",
    "2025-12-10",
    "2025-12-11"
  ],
  "confirmedTrend": [10, 15, 12, 18, 20, 15, 10],
  "suspectedTrend": [20, 25, 22, 28, 30, 25, 20],
  "recoveredTrend": [5, 8, 6, 10, 12, 8, 5],
  "statusDistribution": [
    {
      "value": 5000,
      "name": "正常"
    },
    {
      "value": 2000,
      "name": "疑似"
    },
    {
      "value": 800,
      "name": "确诊"
    },
    {
      "value": 200,
      "name": "康复"
    }
  ],
  "flowRegions": [
    "北京市",
    "上海市",
    "广东省"
  ],
  "flowCounts": [100, 80, 60],
  "movements": [
    {
      "id": 1,
      "personId": 1,
      "name": "张三",
      "avatar": "/api/avatars/avatar_1.jpg",
      "from": "北京市",
      "to": "上海市",
      "time": "2025-12-11 10:30:00",
      "status": "正常"
    }
  ]
}
```

**错误响应** (500)
```json
{
  "error": "获取态势数据失败",
  "message": "详细错误信息",
  "traceback": "错误堆栈信息"
}
```

---

## 7. 静态资源接口

### 7.1 获取头像图片

获取人员头像图片。

**请求**
```
GET /api/avatars/<filename>
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| filename | String | 是 | 头像文件名，如 avatar_1.jpg |

**响应**

返回图片文件（image/jpeg）

**示例**
```
GET /api/avatars/avatar_1.jpg
```

---

## 8. 数据模型

### 8.1 人员对象 (Person)

```json
{
  "id": 1,
  "name": "张三",
  "idCard": "110101199001011234",
  "region": "北京市",
  "hometown": "北京市",
  "age": 30,
  "phone": "13800138000",
  "status": "正常",
  "gender": "男",
  "occupation": "工程师",
  "avatar": "/api/avatars/avatar_1.jpg",
  "tags": ["技术", "北京"],
  "educationHistory": [
    {
      "school": "北京大学",
      "degree": "本科",
      "major": "计算机科学",
      "startDate": "2010-09",
      "endDate": "2014-06"
    }
  ],
  "workHistory": [
    {
      "company": "某科技公司",
      "position": "软件工程师",
      "startDate": "2014-07",
      "endDate": "2018-12"
    }
  ],
  "visitRecords": [
    {
      "province": "上海市",
      "city": "上海市",
      "visitDate": "2025-11-15"
    }
  ],
  "flightRecords": [
    {
      "airline": "中国国际航空",
      "flightNumber": "CA1234",
      "from": "北京",
      "to": "上海",
      "date": "2025-11-15"
    }
  ],
  "trainRecords": [
    {
      "type": "高铁",
      "trainNumber": "G123",
      "from": "北京",
      "to": "上海",
      "date": "2025-11-20"
    }
  ],
  "socialMedia": [
    {
      "platform": "微信",
      "account": "zhangsan",
      "lastActive": "2025-12-10"
    }
  ],
  "lastUpdate": "2025-12-11 10:30:00",
  "createdAt": "2025-01-01 00:00:00"
}
```

### 8.2 流动记录对象 (Movement)

```json
{
  "id": 1,
  "personId": 1,
  "name": "张三",
  "avatar": "/api/avatars/avatar_1.jpg",
  "from": "北京市",
  "to": "上海市",
  "time": "2025-12-11 10:30:00",
  "status": "正常"
}
```

### 8.3 重点人员对象 (KeyPerson)

```json
{
  "id": 1,
  "keyPersonId": 1,
  "personId": 1,
  "name": "张三",
  "category": "高风险人员",
  "priorityLevel": 5,
  "reason": "近期有高风险地区旅居史",
  "addedAt": "2025-12-01 10:00:00",
  "updatedAt": "2025-12-01 10:00:00"
}
```

---

## 9. 错误码说明

### 9.1 HTTP状态码

| 状态码 | 说明 | 处理建议 |
|--------|------|----------|
| 200 | 请求成功 | - |
| 201 | 创建成功 | - |
| 400 | 请求参数错误 | 检查请求参数是否正确 |
| 404 | 资源不存在 | 检查资源ID是否正确 |
| 500 | 服务器内部错误 | 联系管理员或查看服务器日志 |

### 9.2 业务错误码

当前版本使用HTTP状态码表示错误，未来可能扩展业务错误码。

---

## 10. 请求示例

### 10.1 使用curl

**获取统计数据**:
```bash
curl http://localhost:8000/api/stats
```

**创建人员**:
```bash
curl -X POST http://localhost:8000/api/people \
  -H "Content-Type: application/json" \
  -d '{
    "name": "李四",
    "idCard": "110101199002021234",
    "hometown": "北京市",
    "age": 29,
    "phone": "13900139000",
    "status": "正常"
  }'
```

**更新人员**:
```bash
curl -X PUT http://localhost:8000/api/people/1 \
  -H "Content-Type: application/json" \
  -d '{
    "age": 31,
    "status": "疑似"
  }'
```

**删除人员**:
```bash
curl -X DELETE http://localhost:8000/api/people/1
```

### 10.2 使用JavaScript (Axios)

```javascript
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:8000/api',
  timeout: 10000
})

// 获取统计数据
const getStats = async () => {
  try {
    const response = await api.get('/stats')
    console.log(response.data)
  } catch (error) {
    console.error('错误:', error)
  }
}

// 创建人员
const createPerson = async () => {
  try {
    const response = await api.post('/people', {
      name: '李四',
      idCard: '110101199002021234',
      hometown: '北京市',
      age: 29,
      phone: '13900139000',
      status: '正常'
    })
    console.log(response.data)
  } catch (error) {
    console.error('错误:', error)
  }
}
```

### 10.3 使用Python (requests)

```python
import requests

BASE_URL = 'http://localhost:8000/api'

# 获取统计数据
response = requests.get(f'{BASE_URL}/stats')
print(response.json())

# 创建人员
data = {
    'name': '李四',
    'idCard': '110101199002021234',
    'hometown': '北京市',
    'age': 29,
    'phone': '13900139000',
    'status': '正常'
}
response = requests.post(f'{BASE_URL}/people', json=data)
print(response.json())
```

---

## 11. 注意事项

### 11.1 数据初始化
- 首次访问API时，如果数据库为空，系统会自动初始化假数据
- 初始化过程可能需要几秒钟，请耐心等待

### 11.2 数据同步
- 创建、更新、删除人员后，系统会自动同步地图数据
- 地图数据会根据实际人员分布自动更新

### 11.3 头像路径
- 头像路径为相对路径，如 `/api/avatars/avatar_1.jpg`
- 访问头像时使用完整URL: `http://localhost:8000/api/avatars/avatar_1.jpg`

### 11.4 时间格式
- 所有时间字段使用字符串格式: `YYYY-MM-DD HH:MM:SS`
- 日期字段使用格式: `YYYY-MM-DD`

### 11.5 分页
- 当前版本未实现分页，获取人员列表时会返回所有数据
- 建议前端实现分页或虚拟滚动以优化性能

### 11.6 性能建议
- 大数据量查询时，建议使用筛选条件减少返回数据量
- 态势感知接口数据量较大，建议合理设置缓存

---

## 12. 更新日志

### v1.0.0 (2025-12-11)
- 初始版本发布
- 实现人员管理、重点人员管理、态势感知等核心功能
- 支持统计数据、地图数据可视化

---

## 13. 联系方式

如有问题或建议，请联系开发团队。

---

**文档版本**: v1.0.0  
**最后更新**: 2025-12-11

